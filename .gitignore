/.opencode
/.agents
node_modules/
dist/
**/dist/
/graphify-out
/_self
# Ignore all files starting with .env anywhere in the repository
.env*
**/.env*

# Prevent ignoring common local environment directories named "env"
!**/env/

# Whitelist environment templates so team members can see required keys
!*.env.example
!*.env.template
!**/*.env.example
!**/*.env.template
/.reasonix
/.ua
/package-lock.json
/.zcode
/.playwright-mcp
package-lock.json

# Python bytecode and virtual environments (AI valuation sidecar)
__pycache__/
*.pyc
.venv/
venv/
**/env/
**/.venv/

# AI valuation: copies of datasets uploaded via the Retrain page (written on every retrain)
data_layer/ai_model_repository/datasets/uploads/
# Playwright CLI output (may contain credentials)
.playwright-cli/
/.claude

/_herman-only
/.wolf
GEMINI.md
/data_layer/document_storage
*.pdf
